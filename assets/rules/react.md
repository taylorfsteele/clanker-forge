# React Rules

- Never write a useEffect hook. Invoke the /react-no-use-effect skill for further guidance
- Never write `setTimeout`. It is almost always wrong. If this API is truly needed, which is almost never is, use an applicable hook from a hook library like `react-use` or `react-hookz/web` instead.